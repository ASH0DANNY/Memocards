// iOS stub — see FlashCardWidget.ios.tsx and iosWidgetSync.ios.ts for the
// real iOS widget path, and iosWidgetSync.android.ts for the mirror image
// of this same fix on the other platform.
export async function syncAndroidWidget(): Promise<void> {
  // no-op on iOS
}
